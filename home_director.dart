import 'package:flutter/material.dart';


class DirectorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Director')),
      body: Center(child: Text('Página de Director')),
    );
  }
}